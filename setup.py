# -*- coding: utf-8 -*-

import json
from setuptools import find_packages, setup


setup(
    name='bibjsontools',
    version='0.5a',
    packages=find_packages(),
    install_requires=[],
)
